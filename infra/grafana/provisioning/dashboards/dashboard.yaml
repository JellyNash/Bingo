# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'Bingo Platform Dashboards'
    orgId: 1
    folder: 'Bingo'
    folderUid: 'bingo'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json