# Bingo Platform - Cloud Production Environment
# Copy this file to .env.cloud and update ALL values

# Security - MUST CHANGE THESE IN PRODUCTION
JWT_SECRET=CHANGE-THIS-TO-A-SECURE-RANDOM-STRING-AT-LEAST-32-CHARACTERS
GAME_SEED_SECRET=CHANGE-THIS-TO-ANOTHER-SECURE-RANDOM-STRING-AT-LEAST-32-CHARACTERS

# Database (use managed service in production)
# For managed DB: postgresql://username:password@host:5432/dbname
DATABASE_URL=postgresql://bingo:CHANGE-THIS-PASSWORD@postgres:5432/bingo
POSTGRES_PASSWORD=CHANGE-THIS-PASSWORD

# Redis
REDIS_URL=redis://redis:6379
EVENT_CHANNEL=bingo:events

# Services
NODE_ENV=production
API_PORT=3000
REALTIME_PORT=4000

# Caddy (Web Server) - REQUIRED FOR CLOUD
DOMAIN=your-domain.com  # CHANGE THIS to your actual domain
EMAIL=admin@your-domain.com  # CHANGE THIS to your email for Let's Encrypt

# Grafana (Monitoring)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE-THIS-SECURE-PASSWORD
GF_SERVER_ROOT_URL=https://your-domain.com:3001

# Security headers for production
SECURITY_HEADERS="Strict-Transport-Security max-age=31536000; includeSubDomains; preload; X-Content-Type-Options nosniff; X-Frame-Options DENY; X-XSS-Protection 1; mode=block; Referrer-Policy strict-origin-when-cross-origin; Permissions-Policy interest-cohort=()"

# Optional: External services
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# ANALYTICS_ID=G-XXXXXXXXXX

# Optional: Resource limits
# API_MEMORY_LIMIT=512M
# REALTIME_MEMORY_LIMIT=256M

# Notes:
# 1. Generate secure secrets using: openssl rand -base64 32
# 2. Set up DNS A record pointing to your server IP
# 3. Ensure ports 80 and 443 are open for HTTPS
# 4. Consider using Docker secrets for sensitive values
# 5. Use managed PostgreSQL service for production (RDS, Cloud SQL, etc.)