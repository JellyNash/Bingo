{
  "$id": "draw.next",
  "description": "A new ball/number is drawn in the game",
  "allOf": [
    { "$ref": "_common.json" },
    {
      "properties": {
        "name": { "const": "draw.next" },
        "props": {
          "type": "object",
          "required": ["seq", "value"],
          "properties": {
            "seq": { 
              "type": "integer", 
              "minimum": 1,
              "description": "Draw sequence number"
            },
            "value": { 
              "type": "integer", 
              "minimum": 1, 
              "maximum": 75,
              "description": "Ball/number drawn"
            },
            "latencyMsFromServer": { 
              "type": "number",
              "minimum": 0,
              "description": "Client-measured latency from server emit to receive"
            }
          },
          "additionalProperties": true
        }
      }
    }
  ]
}